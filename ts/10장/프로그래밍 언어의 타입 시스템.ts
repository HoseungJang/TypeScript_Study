/*
타입 검사 방식

1. 동적 타입 검사
- 런타임에서 검사

2. 정작 타입 검사
- 컴파일 타임에서 검사

타입스크립트는 위의 두 가지 방식을 모두 지원한다.

타입 호환 방식

1. 구조 타입 시스템
- 구조와 정의가 같다면 호환

2. 명목 타입 시스템
- 명시적으로 같은 타입이어야만 호환

타입스크립트의 타이핑 방식

1. 덕 타이핑
- 런타임 시에 동적으로 할당된 객체의 타입을 검사하는 것

2. 구조 타이핑
- 두 타입의 구조가 같으면 타입 호환

3. 구조 서브타이핑
- 구조가 부분적으로 같으면 타입 호환

4. 명목 타이핑
*/