/*
객체 타입
Array
Tuple
Function
생성자
Class
Interface
*/

// array
// 타입 뒤에 [] 를 붙여서 사용
let items: number[] = [1, 2, 3];

// tuple
// 배열 요소가 n개로 정해질 때, 각 요소별로 타입을 지정한 타입
let x: [string, number];
x = ["JangHoseung", 18]; // 초기화는 순서에 맞게 해야함.

// Function 타입은 6장에서

// 생성자 타입은 7장에서